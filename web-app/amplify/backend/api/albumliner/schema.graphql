type Review @model @searchable @auth(rules: [
  { allow: public, operations: [read]}
  { allow: owner }]) {
  id: ID!
  date: Int!
  userID: String!
  title: String
  body: String
  rating: Int
  albumName: String
  artistName: String
  albumID: String
}